import { ApplyRequestsDto, BatchDto, ChainError, ContractAPI, DryRunDto, DryRunResultDto, ErrorCode, GalaChainErrorResponse, GalaChainResponse, GetObjectDto, GetObjectHistoryDto, HasPendingApplyRequestsDto, ValidationFailedError } from "@gala-chain/api";
import { Contract } from "fabric-contract-api";
import { GalaChainContext, GalaChainContextConfig } from "../types";
import type { RequestMethodHandler } from "./GalaTransactionRequest";
export declare class BatchWriteLimitExceededError extends ValidationFailedError {
    constructor(writesLimit: number);
}
export declare class BatchPartialSuccessRequiredError extends ChainError {
    readonly code: ErrorCode;
    constructor(index: number, error: GalaChainErrorResponse<unknown>);
}
export declare abstract class GalaContract extends Contract {
    private readonly version;
    private readonly config;
    protected readonly requestMethodHandlers: Record<string, RequestMethodHandler>;
    /**
     * @param name Contract name
     * @param version Contract version. The actual value should be defined in the child
     *    * class, and should be taken from package.json. If you extend contract class
     *    * that extends GalaContract, you should also override version.
     */
    constructor(name: string, version: string, config?: GalaChainContextConfig);
    getVersion(): string;
    createContext(): GalaChainContext;
    beforeTransaction(ctx: GalaChainContext): Promise<void>;
    aroundTransaction(ctx: GalaChainContext, fn: Function, parameters: unknown): Promise<void>;
    afterTransaction(ctx: GalaChainContext, result: unknown): Promise<void>;
    GetContractVersion(ctx: GalaChainContext): Promise<string>;
    GetChaincodeVersion(ctx: GalaChainContext): Promise<string>;
    GetContractAPI(ctx: GalaChainContext): Promise<ContractAPI>;
    getContractAPI(): ContractAPI;
    GetObjectByKey(ctx: GalaChainContext, dto: GetObjectDto): Promise<Record<string, unknown>>;
    GetObjectHistory(ctx: GalaChainContext, dto: GetObjectHistoryDto): Promise<Record<string, unknown>>;
    DryRun(ctx: GalaChainContext, dto: DryRunDto): Promise<DryRunResultDto>;
    BatchSubmit(ctx: GalaChainContext, batchDto: BatchDto): Promise<GalaChainResponse<unknown>[]>;
    ApplyRequests(ctx: GalaChainContext, dto: ApplyRequestsDto): Promise<GalaChainResponse<unknown>[]>;
    HasPendingApplyRequests(ctx: GalaChainContext, dto: HasPendingApplyRequestsDto): Promise<boolean>;
    BatchEvaluate(ctx: GalaChainContext, batchDto: BatchDto): Promise<GalaChainResponse<unknown>[]>;
}
//# sourceMappingURL=GalaContract.d.ts.map