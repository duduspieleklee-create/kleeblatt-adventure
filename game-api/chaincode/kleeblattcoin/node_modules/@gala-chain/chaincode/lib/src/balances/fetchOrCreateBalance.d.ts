import { NonFunctionProperties, TokenBalance, TokenClassKey, UserAlias } from "@gala-chain/api";
import { GalaChainContext } from "../types";
/**
 * @description
 *
 * Query a single `TokenBalance` from on-chain World State
 * belonging to the provided `owner` for the specified
 * `tokenClassKey` argument.
 *
 * If the `TokenBalance` does not yet exist on-chain, a
 * new `TokenBalance` class instance will be instantiated
 * and returned.
 *
 * @param ctx
 * @param owner
 * @param tokenClassKey
 * @returns Promise<TokenBalance>
 */
export declare function fetchOrCreateBalance(ctx: GalaChainContext, owner: UserAlias, tokenClassKey: NonFunctionProperties<TokenClassKey>): Promise<TokenBalance>;
//# sourceMappingURL=fetchOrCreateBalance.d.ts.map