import { TokenInstanceQuantity, TokenSwapRequest, UserAlias } from "@gala-chain/api";
import { BigNumber } from "bignumber.js";
import { GalaChainContext } from "../types";
interface RequestTokenSwapParams {
    offeredBy: UserAlias;
    offeredTo?: UserAlias;
    offered: TokenInstanceQuantity[];
    wanted: TokenInstanceQuantity[];
    uses: BigNumber;
    expires: number;
}
export declare function requestTokenSwap(ctx: GalaChainContext, { offeredBy, offeredTo, offered, wanted, uses, expires }: RequestTokenSwapParams): Promise<TokenSwapRequest>;
export {};
//# sourceMappingURL=requestTokenSwap.d.ts.map