import { ExpectedTokenSwap, TokenSwapFill, UserAlias } from "@gala-chain/api";
import BigNumber from "bignumber.js";
import { GalaChainContext } from "../types";
export interface SwapFillParams {
    swapRequestId: string;
    filledBy: UserAlias;
    uses: BigNumber;
    expectedTokenSwap?: ExpectedTokenSwap | undefined;
}
export declare function batchFillTokenSwaps(ctx: GalaChainContext, swapFills: SwapFillParams[]): Promise<TokenSwapFill[]>;
//# sourceMappingURL=batchFillTokenSwaps.d.ts.map