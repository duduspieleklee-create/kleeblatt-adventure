import { FeeGateCodes } from "@gala-chain/api";
import { GalaChainContext } from "../types";
export interface IExemptionForUser {
    user: string;
    feeCode: FeeGateCodes;
}
/**
 * @description
 *
 * Returns true if a given user is exempt from the given fee code.
 *
 * @param ctx
 * @param data
 * @returns Promise<boolean>
 */
export declare function userExemptFromFees(ctx: GalaChainContext, data: IExemptionForUser): Promise<boolean>;
//# sourceMappingURL=userExemptFromFees.d.ts.map