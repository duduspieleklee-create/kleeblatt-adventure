import { FeeCodeDefinition, FetchFeeScheduleResDto } from "@gala-chain/api";
import BigNumber from "bignumber.js";
import { GalaChainContext } from "../types";
export interface FetchFeeScheduleParams {
    feeCode?: string;
    feeThresholdUses?: BigNumber;
    bookmark?: string;
    limit?: number;
}
/**
 * Provide no `feeCode` paramenter to return the entire fee schedule.
 * Define a `feeCode` value to lookup the `FeeCodeDefinition` of a specific code.
 *
 * @param ctx
 * @param data
 * @returns
 */
export declare function fetchFeeSchedule(ctx: GalaChainContext, data: FetchFeeScheduleParams): Promise<FetchFeeScheduleResDto>;
export declare function compareFeeThresholds(a: FeeCodeDefinition, b: FeeCodeDefinition): number;
export declare function sortFeeDefinitionsByThreshold(results: FeeCodeDefinition[]): Promise<void>;
export declare function sortFeeDefinitionsByCode(results: FeeCodeDefinition[]): Promise<void>;
//# sourceMappingURL=fetchFeeSchedule.d.ts.map