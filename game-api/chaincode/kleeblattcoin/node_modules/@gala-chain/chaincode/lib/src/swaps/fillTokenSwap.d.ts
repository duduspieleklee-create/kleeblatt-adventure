import { ExpectedTokenSwap, TokenSwapFill, UserAlias } from "@gala-chain/api";
import { BigNumber } from "bignumber.js";
import { GalaChainContext } from "../types";
interface FillTokenSwapParams {
    filledBy: UserAlias;
    uses: BigNumber;
    swapRequestId: string;
    expectedTokenSwap?: ExpectedTokenSwap | undefined;
}
export declare function fillTokenSwap(ctx: GalaChainContext, { filledBy, uses, swapRequestId, expectedTokenSwap }: FillTokenSwapParams): Promise<TokenSwapFill>;
export {};
//# sourceMappingURL=fillTokenSwap.d.ts.map