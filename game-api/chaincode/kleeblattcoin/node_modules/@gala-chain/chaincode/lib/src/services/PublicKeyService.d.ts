import { ChainCallDTO, PublicKey, UserAlias, UserProfileStrict, normalizePublicKey } from "@gala-chain/api";
import { Context } from "fabric-contract-api";
import { GalaChainContext } from "../types";
export declare class PublicKeyService {
    private static PK_INDEX_KEY;
    private static UP_INDEX_KEY;
    static getPublicKeyKey(ctx: Context, userAlias: string): string;
    static getUserProfileKey(ctx: Context, address: string): string;
    static normalizePublicKey: typeof normalizePublicKey;
    static putPublicKey(ctx: GalaChainContext, publicKey: string, userAlias: string): Promise<PublicKey>;
    static putUserProfile(ctx: GalaChainContext, userAlias: UserAlias, roles: string[] | undefined, address: {
        ethAddress?: string;
    } | undefined, signers: UserAlias[] | undefined, signatureQuorum: number): Promise<void>;
    static invalidateUserProfile(ctx: GalaChainContext, address: string): Promise<void>;
    static getUserAddress(publicKey: string): string;
    static getUserProfile(ctx: GalaChainContext, address: string): Promise<UserProfileStrict | undefined>;
    static getDefaultPublicKey(publicKey: string): PublicKey;
    static getDefaultUserProfile(address: string): UserProfileStrict;
    static getPublicKey(ctx: GalaChainContext, userId: UserAlias): Promise<PublicKey | undefined>;
    static registerUser(ctx: GalaChainContext, publicKey: string | undefined, signers: UserAlias[] | undefined, userAlias: UserAlias, signatureQuorum: number, dto?: ChainCallDTO & {
        publicKeySignature?: string;
    }): Promise<string>;
    static updatePublicKey(ctx: GalaChainContext, dto: {
        publicKey: string;
        publicKeySignature?: string;
    }): Promise<void>;
    static updateUserRoles(ctx: GalaChainContext, user: UserAlias, roles: string[]): Promise<void>;
    static addSigner(ctx: GalaChainContext, newSigner: UserAlias): Promise<void>;
    static removeSigner(ctx: GalaChainContext, signerToRemove: UserAlias): Promise<void>;
    static updateQuorum(ctx: GalaChainContext, newQuorum: number): Promise<void>;
    /**
     * Verifies if the data is properly signed. Throws exception instead.
     */
    static ensurePublicKeySignatureIsValid(ctx: GalaChainContext, userAlias: UserAlias, dto: ChainCallDTO): Promise<PublicKey>;
}
//# sourceMappingURL=PublicKeyService.d.ts.map