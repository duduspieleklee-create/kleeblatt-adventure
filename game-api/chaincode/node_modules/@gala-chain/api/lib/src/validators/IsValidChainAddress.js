"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.IsValidChainAddress = exports.isValidChainAddress = exports.validateChainAddress = exports.meansValidChainAddress = exports.ChainAddressValidationResult = void 0;
const tslib_1 = require("tslib");
/*
 * Copyright (c) Gala Games Inc. All rights reserved.
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
const class_validator_1 = require("class-validator");
const utils_1 = require("../utils");
const IsUserAlias_1 = require("./IsUserAlias");
var ChainAddressValidationResult;
(function (ChainAddressValidationResult) {
    ChainAddressValidationResult[ChainAddressValidationResult["VALID_ETHEREUM"] = 0] = "VALID_ETHEREUM";
    ChainAddressValidationResult[ChainAddressValidationResult["VALID_SOLANA"] = 1] = "VALID_SOLANA";
    ChainAddressValidationResult[ChainAddressValidationResult["VALID_TON"] = 2] = "VALID_TON";
    ChainAddressValidationResult[ChainAddressValidationResult["VALID_GALACHAIN"] = 3] = "VALID_GALACHAIN";
    ChainAddressValidationResult[ChainAddressValidationResult["INVALID_ETHEREUM"] = 4] = "INVALID_ETHEREUM";
    ChainAddressValidationResult[ChainAddressValidationResult["INVALID_SOLANA"] = 5] = "INVALID_SOLANA";
    ChainAddressValidationResult[ChainAddressValidationResult["INVALID_TON"] = 6] = "INVALID_TON";
    ChainAddressValidationResult[ChainAddressValidationResult["INVALID_GALACHAIN"] = 7] = "INVALID_GALACHAIN";
    ChainAddressValidationResult[ChainAddressValidationResult["INVALID_FORMAT"] = 8] = "INVALID_FORMAT";
})(ChainAddressValidationResult || (exports.ChainAddressValidationResult = ChainAddressValidationResult = {}));
function meansValidChainAddress(result) {
    return (result === ChainAddressValidationResult.VALID_ETHEREUM ||
        result === ChainAddressValidationResult.VALID_SOLANA ||
        result === ChainAddressValidationResult.VALID_TON ||
        result === ChainAddressValidationResult.VALID_GALACHAIN);
}
exports.meansValidChainAddress = meansValidChainAddress;
// Base58 alphabet used by Solana
const BASE58_ALPHABET = "123456789ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz";
function base58Decode(input) {
    if (!input || input.length === 0) {
        return null;
    }
    // Check if all characters are valid base58
    for (const char of input) {
        if (!BASE58_ALPHABET.includes(char)) {
            return null;
        }
    }
    try {
        const base = BigInt(BASE58_ALPHABET.length);
        let num = BigInt(0);
        // Count leading zeros (encoded as '1' in base58)
        let leadingZeros = 0;
        for (let i = 0; i < input.length && input[i] === "1"; i++) {
            leadingZeros++;
        }
        // Convert base58 string to BigInt (skip leading ones)
        const significantPart = input.slice(leadingZeros);
        if (significantPart.length === 0) {
            // All zeros case
            return new Uint8Array(leadingZeros);
        }
        for (const char of significantPart) {
            const index = BASE58_ALPHABET.indexOf(char);
            if (index === -1) {
                return null;
            }
            num = num * base + BigInt(index);
        }
        // Convert BigInt to bytes
        const bytes = [];
        if (num === BigInt(0)) {
            return new Uint8Array(leadingZeros);
        }
        while (num > BigInt(0)) {
            bytes.push(Number(num % BigInt(256)));
            num = num / BigInt(256);
        }
        const significantBytes = new Uint8Array(bytes.reverse());
        // Combine leading zeros with significant bytes
        const result = new Uint8Array(leadingZeros + significantBytes.length);
        result.set(significantBytes, leadingZeros);
        return result;
    }
    catch {
        return null;
    }
}
function isValidSolanaAddress(address) {
    // Solana addresses are base58 encoded 32-byte public keys
    // Typical length is 32-44 characters
    if (!address || address.length < 32 || address.length > 44) {
        return false;
    }
    const decoded = base58Decode(address);
    if (!decoded) {
        return false;
    }
    // Solana public keys must decode to exactly 32 bytes
    // Base58 encoding of 32 bytes can result in variable length strings,
    // but when decoded, it must be exactly 32 bytes (leading zeros are encoded as '1's)
    return decoded.length === 32;
}
function isValidTonAddress(address) {
    // TON addresses must be non-bounceable and not test-only (mainnet only)
    // Friendly format: UQ... (48 characters, base64url encoded)
    // U = non-bounceable mainnet, Q = continuation (part of base64url encoding)
    // We require: isBounceable = false && isTestOnly = false
    // Check for non-bounceable mainnet format: UQ followed by base64url characters
    if (!/^UQ[a-zA-Z0-9_-]{46}$/.test(address)) {
        return false;
    }
    // Validate base64url encoding and decode to check flags
    try {
        // Base64url alphabet: A-Za-z0-9_- (no + or /)
        if (!/^[A-Za-z0-9_-]+$/.test(address)) {
            return false;
        }
        // Decode the entire address string as base64url
        const base64 = address.replace(/-/g, "+").replace(/_/g, "/");
        const padding = base64.length % 4;
        const paddedBase64 = base64 + "=".repeat(padding === 0 ? 0 : 4 - padding);
        const decoded = Buffer.from(paddedBase64, "base64");
        // TON address structure: 1 byte flags + 1 byte workchain + 32 bytes address hash + 2 bytes checksum = 36 bytes total
        // Base64url encoded: 36 * 4/3 = 48 characters
        if (decoded.length !== 36) {
            return false;
        }
        // Check flags byte (first byte)
        // Bit 6 (0x40): bounceable flag (1 = non-bounceable, 0 = bounceable)
        // Bit 7 (0x80): testnet flag (1 = testnet/test-only, 0 = mainnet)
        const flags = decoded[0];
        const isNonBounceable = (flags & 0x40) !== 0;
        const isTestOnly = (flags & 0x80) !== 0;
        // Must be non-bounceable and not test-only
        return isNonBounceable && !isTestOnly;
    }
    catch {
        return false;
    }
}
function isValidEthereumAddress(address) {
    // Ethereum addresses must have 0x prefix and be checksummed
    if (!address.startsWith("0x")) {
        return false;
    }
    // Use the existing validation from signatures
    return utils_1.signatures.isChecksumedEthAddress(address);
}
function validateChainAddress(value) {
    if (typeof value !== "string" || value.length === 0) {
        return ChainAddressValidationResult.INVALID_FORMAT;
    }
    // Check Ethereum first (has 0x prefix, easy to identify)
    if (value.startsWith("0x")) {
        if (isValidEthereumAddress(value)) {
            return ChainAddressValidationResult.VALID_ETHEREUM;
        }
        else {
            return ChainAddressValidationResult.INVALID_ETHEREUM;
        }
    }
    // Check TON (starts with UQ for non-bounceable mainnet)
    if (value.startsWith("UQ") || value.startsWith("EQ") || value.startsWith("0:")) {
        if (isValidTonAddress(value)) {
            return ChainAddressValidationResult.VALID_TON;
        }
        else {
            return ChainAddressValidationResult.INVALID_TON;
        }
    }
    // Check Solana (base58 encoded, 32-44 chars, doesn't start with special prefixes)
    if (isValidSolanaAddress(value)) {
        return ChainAddressValidationResult.VALID_SOLANA;
    }
    // Check GalaChain (same as IsUserAlias)
    if ((0, IsUserAlias_1.isValidUserAlias)(value)) {
        return ChainAddressValidationResult.VALID_GALACHAIN;
    }
    // If we got here and it looks like Solana format but failed validation
    if (value.length >= 32 && value.length <= 44) {
        // Check if it contains only base58 characters
        let allBase58 = true;
        for (const char of value) {
            if (!BASE58_ALPHABET.includes(char)) {
                allBase58 = false;
                break;
            }
        }
        if (allBase58) {
            return ChainAddressValidationResult.INVALID_SOLANA;
        }
    }
    // If it looks like TON but failed
    if (value.startsWith("UQ") || value.startsWith("EQ") || value.startsWith("0:")) {
        return ChainAddressValidationResult.INVALID_TON;
    }
    // If it looks like Ethereum but failed
    if (/^0x[0-9a-fA-F]{40}$/.test(value)) {
        return ChainAddressValidationResult.INVALID_ETHEREUM;
    }
    return ChainAddressValidationResult.INVALID_FORMAT;
}
exports.validateChainAddress = validateChainAddress;
function isValidChainAddress(value) {
    const result = validateChainAddress(value);
    return meansValidChainAddress(result);
}
exports.isValidChainAddress = isValidChainAddress;
const customMessages = {
    [ChainAddressValidationResult.INVALID_ETHEREUM]: "Ethereum address must be checksummed and start with '0x' prefix.",
    [ChainAddressValidationResult.INVALID_SOLANA]: "Solana address must be a valid base58-encoded 32-byte public key.",
    [ChainAddressValidationResult.INVALID_TON]: "TON address must be non-bounceable and not test-only (UQ... format).",
    [ChainAddressValidationResult.INVALID_GALACHAIN]: "GalaChain address must be a valid user alias."
};
const genericMessage = "Expected a valid chain address: Ethereum (checksummed with 0x prefix), " +
    "Solana (base58-encoded 32-byte public key), TON (non-bounceable mainnet format: UQ...), " +
    "or GalaChain (valid user alias).";
let IsValidChainAddressConstraint = class IsValidChainAddressConstraint {
    validate(value, args) {
        if (Array.isArray(value)) {
            return value.every((val) => this.validate(val, args));
        }
        const result = validateChainAddress(value);
        return meansValidChainAddress(result);
    }
    defaultMessage(args) {
        var _a;
        const value = args.value;
        if (Array.isArray(value)) {
            const invalidValues = value.filter((val) => !meansValidChainAddress(validateChainAddress(val)));
            return `${args.property} property with values ${invalidValues} are not valid chain addresses. ${genericMessage}`;
        }
        const result = validateChainAddress(args.value);
        const details = (_a = customMessages[result]) !== null && _a !== void 0 ? _a : genericMessage;
        return `${args.property} property with value ${args.value} is not a valid chain address. ${details}`;
    }
};
IsValidChainAddressConstraint = tslib_1.__decorate([
    (0, class_validator_1.ValidatorConstraint)({ async: false })
], IsValidChainAddressConstraint);
/**
 * @description
 *
 * Used to register a decorator for class validation.
 * Validates against IsValidChainAddressConstraint.
 * Supports addresses for Ethereum, Solana, TON, and GalaChain chains.
 *
 * Ethereum addresses must be checksummed and start with '0x' prefix.
 * Solana addresses must be base58-encoded 32-byte public keys.
 * TON addresses must be non-bounceable and not test-only (UQ... format).
 * GalaChain addresses must be valid user aliases.
 *
 * @param options
 *
 */
function IsValidChainAddress(options) {
    return function (object, propertyName) {
        (0, class_validator_1.registerDecorator)({
            name: "isValidChainAddress",
            target: object.constructor,
            propertyName,
            options,
            constraints: [],
            validator: IsValidChainAddressConstraint
        });
    };
}
exports.IsValidChainAddress = IsValidChainAddress;
//# sourceMappingURL=IsValidChainAddress.js.map