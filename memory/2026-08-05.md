# 2026-08-05

## Enemy AI FSM (#3) — DONE
- Workboard card #3 (id `3dd27b58-5715-47f1-953c-88c5cecc17ea`) completed → **PR #104**
- Implemented FSM `idle → chase → attack → leash → dead` in `apps/web/src/game/scenes/match-scene.ts`
- Three enemy types via `ENEMY_STATS`: bruiser (slow/tanky melee), runner (fast skirmisher), spitter (keeps 100–180px preferred range, ranged)
- Per-type aggro (`detectRange`), leash-back + full heal, per-type attack range/cooldown, emits `player:hz` damage events
- `createSkeleton(enemyType)` + `respawnEnemy()` re-anchors spawn

## Rebase lesson
- First version of PR #104 was branched off `feat/react-inventory-screen-41` (old inventory work) → diff polluted with 5 unrelated commits (Loot API #52, Inventory #41, Char Creation #40, Map)
- Fixed via `git reset --hard origin/main` + re-applying the FSM edit manually on main's `match-scene.ts` (which uses `createWorldMap` helper + `worldW/worldH` getters — different from old branch's `this.map.widthInPixels`)
- `git push --force-with-lease` → PR #104 now single-commit, single-file ✅

## Branch workboard state
- PR #99 (Inventory) already MERGED; main has newer InventoryScreen (uses `../lib/gameBridge`, `GameEvents`, items.json) — my API-rewrite commit a2594b01 on old branch is superseded, harmless
- Card #3 marked done with proof URL

## Gotchas encountered
- match-scene.ts had uncommitted corruption from previous session (stray `});` + duplicated createMap block) — repaired during this work
- `gh pr edit` broken (Projects-classic) → use `gh api -X PATCH .../pulls/N --input body.json`
- Workspace scratch files (`.mjs`, `.bak`, temp_methods.txt) cleaned up

## Equip wirkt auf Held-Stats (#53) — DONE → PR #105
- New shared module `packages/shared/src/stats.ts`: `computeFinalStats(heroClass, level, equippedItems)` — formula base + gear + levelBonus (additiv) per game-config.json statStacking; constants HERO_CLASS_BASE_STATS + HERO_LEVEL_BONUSES (hpPerLevel 12, atkPerLevel 2); 4 unit tests
- HomePage: `equippedStats` now includes base + level bonus + gear (was gear-only)
- MatchScene: sync currentHp/Mana/Stamina to new max after match:start & loadout:update
- Branch `feat/equip-stats-53`, card #53 (id 4ec365ce-0a3b-4735-ac3d-a214ba63e319) done
- Card was stranded_ready (assigned to main, unclaimed) — claimed, implemented, completed in one session

## Current PRs
- #104: enemy AI FSM (card #3) — single-commit after rebase onto origin/main
- #105: equip stats (card #53)
- #90/#96/#97/#99/#100: earlier work (some merged: #99 merged)
- Remaining todo cards for main: #85 tilesets (96347806), #86 crops/animals/VFX (03cabc12), #87 UI sprites → HUD (defcb323)
