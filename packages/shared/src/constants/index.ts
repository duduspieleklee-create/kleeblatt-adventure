/** Shared constants for client + API */

export const API_DEFAULT_PORT = 4000;
export const WEB_DEFAULT_PORT = 5173;

/** Prototype match defaults (see game-config.json → match) */
export const PROTOTYPE_MAP_ID = "prototype_map_01";
