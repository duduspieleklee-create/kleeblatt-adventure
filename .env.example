# --- Prototype stack (apps/api + apps/web) ---
NODE_ENV=development

# API
API_PORT=4000
CORS_ORIGIN=http://localhost:5173
SESSION_SECRET=change-me-in-production-use-long-random-string

# PostgreSQL (docker compose defaults)
DATABASE_URL=postgresql://kleeblatt:kleeblatt@localhost:5432/kleeblatt

# Redis (docker compose defaults)
REDIS_URL=redis://localhost:6379

# Web (Vite)
VITE_API_URL=http://localhost:4000

# --- Legacy / optional (game/ + game-api Python) ---
# GALA_CHAIN_ID=71337
# CORS_ORIGINS=http://localhost:3000
